Dashboard client grid + ranging parse fix

Dashboard:
- Replace Recent Uploads table with a per-client grid: SKUs, YTD units,
  YTD value (sales ex-VAT: Prom SP if >0 else Incl SP/1.15), contribution %
  of total YTD value, DISPO count, aged-stock count, Vital Signs runs,
  Month-End runs.
- New aggregate endpoint app/api/dashboard/clients (view_dashboard, no-store).
- New durable per-client report-run counters (lib/reportCounts.ts); the
  Vital Signs and Month-End routes increment them on successful download.

Ranging parse fix:
- parseRangingSheet now resolves ProductID tolerantly, stripping
  Helper/Mandatory header prefixes (real headers are e.g. HelperProductID),
  so ranging rows are no longer all filtered out. ND Scenario 1 can trigger
  after the ranging file is re-uploaded.
- enrichment _rangingStatus lookup resolves ArticleChannelCode/Article with
  the same tolerant resolver (was keying off a non-existent "Article" header).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
